# R Capital Backend

Node.js + Express backend powering R Capital’s rotation engine, prediction engine, and user settings.

## Setup
npm install  
cp .env.example .env  
npm run dev

## Folder Structure
See /src for all backend logic.

